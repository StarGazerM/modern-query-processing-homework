[package]
name = "cq-ir"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[features]
# Opts into the recommended post-R1 language-extension tests. The production
# language is not conditionally compiled; students extend its real syntax.
hw4 = []

[dependencies]
proc-macro2.workspace = true
quote.workspace = true
syn.workspace = true
syn_derive.workspace = true
